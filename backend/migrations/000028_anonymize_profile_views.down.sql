-- Irreversível de propósito: a identidade dos visitantes foi apagada por
-- decisão de privacidade e não existe mais em lugar nenhum para ser
-- restaurada. Down é um no-op — não há como (nem se deve) desfazer o
-- anonimato.
SELECT 1;
