-- Remove o portfólio de fotos ("Trabalhos realizados") do perfil do
-- prestador. Nunca teve tela de upload no app — o único efeito visível era
-- uma caixa cinza vazia no perfil, sem imagem real, para quem já tinha um
-- registro de teste na tabela.
DROP TABLE provider_photos;
