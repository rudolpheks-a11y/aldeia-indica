<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Debug-only: o backend de desenvolvimento roda em HTTP puro
         (http://10.0.2.2:8081). O Android bloqueia cleartext por padrão desde
         a API 28 — isto libera SÓ na build de debug. A build de release
         continua exigindo HTTPS, então esta permissão não vaza pra produção. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
